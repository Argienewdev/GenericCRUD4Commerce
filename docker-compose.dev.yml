services:
  # -------------------- DATABASE --------------------
  db:
    image: postgres:16-alpine
    container_name: app_db_dev
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: app_db_dev
    ports:
      - "5432:5432"
    volumes:
      - pg_data_dev:/var/lib/postgresql/data
    networks:
      - dev-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin"]
      interval: 5s
      timeout: 5s
      retries: 5

  # -------------------- BACKEND (QUARKUS DEV) --------------------
  backend:
    image: maven:3.9.6-eclipse-temurin-21
    container_name: app_backend_dev
    working_dir: /app
    volumes:
      - ./backend:/app
      - maven_repo:/root/.m2
    environment:
      QUARKUS_PROFILE: dev
      QUARKUS_DATASOURCE_JDBC_URL: jdbc:postgresql://db:5432/app_db_dev
      QUARKUS_DATASOURCE_USERNAME: admin
      QUARKUS_DATASOURCE_PASSWORD: admin
      QUARKUS_HIBERNATE_ORM_DATABASE_GENERATION: update
    ports:
      - "8080:8080"
      - "5005:5005"
    command: bash -c "mvn quarkus:dev -Dquarkus.http.host=0.0.0.0"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - dev-network

  # -------------------- FRONTEND (VITE DEV) --------------------
  frontend:
    image: node:20-alpine
    container_name: app_frontend_dev
    working_dir: /app
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host"
    networks:
      - dev-network

# -------------------- VOLUMES --------------------
volumes:
  pg_data_dev:
  maven_repo:

# -------------------- NETWORKS --------------------
networks:
  dev-network:
